支持 macOS 13+，请按芯片下载 ARM64 或 x86_64 的 DMG。

- 新增 Intel Mac 支持。
- 新增 Sparkle 应用内检查更新及 EdDSA 更新签名。安装前等待导出并恢复系统代理。
- 保留原生逐步证书教程：设置始终信任后，必须关闭证书窗口并输入 Mac 登录密码保存，再检查信任。
- 系统 HTTP/HTTPS 图片采集，按地址、格式或同一文件夹导出。

1.1.1 及更早版本需手动安装本次更新。发行包为 ad-hoc 签名，尚未 Apple Developer ID 签名或公证。
